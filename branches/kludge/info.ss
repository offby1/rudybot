(module info (lib "infotab.ss" "setup")
  (define name "irc-bot")
  (define blurb
        (list "overengineered, underfeatured IRC bot"))
  (define release-notes '(p "Release engineering?  What's that?"))
  (define doc.txt "doc.txt")
  (define primary-file "bot.ss")
  (define categories '(net misc)))
